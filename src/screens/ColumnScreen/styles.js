import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    backgroundColor: "#fff",
    flex: 1,
  },
  loading: {
    fontSize: 30,
    fontWeight: "bold",
    textAlign: "center",
  },
  inputContainer: { alignItems: "center" },
});
