import { StyleSheet } from "react-native";

export default StyleSheet.create({
  inputField: {
    padding: 5,
    fontSize: 20,
    borderWidth: 1,
    borderRadius: 15,
    width: 200,
    textAlign: "center",
  },
});
